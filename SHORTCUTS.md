# Шорткаты и формулы mathNote

Актуально для mathNote и MathLive `0.105.0`.

Это полный список активных сокращений, которые дополнительно поддерживает mathNote. Сам MathLive понимает и другие обычные LaTeX-команды через ввод `\команда`.

## Главное правило автозамен

- Большинство сокращений короче 3 символов рисуется сразу, а от 3 символов — после `Enter`.
- Сразу работают `sum`, `int`, `frac`, `lim`, `iff`, `sin`, `cos`, `tan`, `cot`, `ln`, `log`, `exp`, `arcsin`, `arccos`, `arctan`.
- Исключения для множеств: `in` требует `Enter`, а `!in` и `!EE` срабатывают сразу.
- `deg` → ° и `xin` → \(x\in\) срабатывают сразу; все греческие сокращения требуют `Enter`.
- Буквенные сокращения работают на английской раскладке.

## Управление листом

- `Ctrl+M` — новый блок формулы.
- `Ctrl+H` — новый текстовый блок.
- `Ctrl+Enter` — новый блок-шаг.
- `Ctrl+Shift+Enter` — новый блок рисовалки.
- `Ctrl+S` — сохранить сразу.
- `Ctrl+Z` — отменить.
- `Ctrl+Shift+Z` или `Ctrl+Y` — вернуть.
- Пустой текстовый или формульный блок + `Backspace` — удалить блок.

## Запись формул

- `^` / `Alt+цифра` / `Alt+-` — степень: произвольная / готовая цифровая / отрицательная.
- `_` — индекс: `x_i`.
- `/` / `frac` — дробь сразу. После `x^2` в числитель попадает вся степень.
- `(` и `{` — вставить парные скобки с курсором внутри.
- Закрывающая скобка — выйти за уже созданную пару.
- `Space` — математический пробел.
- `*` / `Shift+8` — ∗ сразу; `***` / `asterisk` — ∗ через `Enter`. В mathNote это `\ast`, а не `\cdot`.
- `\команда` + `Tab` — завершить LaTeX-команду.
- `Alt+R` / `\sqrt` + `Tab` / `sqrt` или `nthroot` + `Enter` — корень с показателем и подкоренным выражением. Если показатель оставить пустым и нажать `Enter`, его поле исчезнет и останется квадратный корень.
- `\int`, `\oint`, `\sum` + `Tab` / `int`, `sum` сразу / `defint` + `Enter` — знак с нижним и верхним пределами.
- `\lim` + `Tab` / `lim` сразу — предел.
- `\` + `Space` — буквальный обратный слеш.

### Enter и перемещение

- В обычной формульной строке `Enter` — новая строка.
- В степени или индексе `Enter` — выйти вправо.
- В скобках `Enter` — выйти за скобку.
- В дроби: числитель → знаменатель → за дробь.
- В корне: показатель → подкоренное выражение → за корень.
- В интеграле или сумме: верхний предел → нижний → на знак.
- В пределе: выражение → значение предела → выйти вперёд.
- В системе `Shift+Enter` — новая строка системы.
- `↓` или `PageDown` — выйти из дроби/степени либо перейти в конец строки; из конца — на следующую строку.
- `Shift+Backspace` — удалить символ слева.
- `Ctrl+Backspace` — удалить до пробела; скобки или дробь удаляются целиком.

### Комментарии внутри формулы

- `#` — начать серый комментарий.
- `Enter` — закончить комментарий и перейти на новую формульную строку.
- `Esc` — закончить комментарий и продолжить формулу в текущей строке.
- В комментарии разрешены обычные пробелы.

### Копирование

- `Ctrl+C` — копировать выделенную формулу как LaTeX.
- `Ctrl+X` — вырезать выделенную формулу как LaTeX.
- `Ctrl+V` — вставить скопированный LaTeX.

## Собственные шорткаты mathNote

### Вставка конструкций

- `Alt+S` — система `\begin{cases}...\end{cases}`.
- `Ctrl+Shift+D` — индекс Δ у символа слева.
- `~` без Shift — ∼ (`\sim`).

### Акценты

- `Shift+~` — открыть выбор акцента над символом слева.
- Повторный `~` — следующий акцент.
- Отпустить `Shift` — применить.
- Доступны `\hat`, `\bar`, `\tilde`.
- `Esc` — закрыть без вставки.

### Меню множеств

- `Ctrl+Alt+C` — открыть меню.
- `U`, `↓`, `→` — следующий символ.
- `Shift+U`, `↑`, `←` — предыдущий.
- `Enter` — вставить.
- `Esc` или `Backspace` — закрыть.
- Символы: ⊂, ∅, ⊆, ⊄, ⊈.

### Меню равенств

- `Ctrl+Alt+=` — открыть меню.
- `=`, `↓`, `→` — следующий символ.
- `Shift+=`, `↑`, `←` — предыдущий.
- `Enter` — вставить.
- `Esc` или `Backspace` — закрыть.
- Символы: ≡, =, ≈, ⇔, ≠, ∼, ≃, ≅, ∝, :=, ≐, ≜, ≊, ≍.

## Греческие сокращения

Все перечисленные сокращения требуют `Enter`. Одинаковые буквы объединены в одну строку; полностью верхний регистр даёт заглавную букву там, где она существует.

- `alp` / `alpha` → α; `ALP` → Α.
- `beta` → β; `BETA` → Β.
- `gam` / `gamma` → γ; `GAM` / `Gamma` → Γ.
- `del` / `delta` → δ; `DEL` / `Delta` → Δ.
- `eps` / `varepsilon` → ε; `epsilon` → ϵ.
- `zeta` → ζ.
- `eta` → η.
- `theta` → θ; `Theta` → Θ; `vartheta` → ϑ.
- `io` / `iota` → ι.
- `kappa` → κ.
- `lam` / `lambda` → λ; `LAM` / `Lambda` → Λ.
- `mu` → μ.
- `nu` → ν.
- `xi` → ξ; `XI` → Ξ.
- `pi` → π; `PI` → Π.
- `rho` → ρ.
- `sig` / `sigma` → σ; `SIG` / `Sigma` → Σ.
- `tau` → τ.
- `ups` / `upsilon` → υ; `UPS` → Υ.
- `phi` / `varphi` → φ; `PHI` → Φ. В mathNote `phi` означает `\varphi`.
- `chi` → χ.
- `psi` → ψ; `PSI` → Ψ.
- `ome` / `omega` → ω; `OME` / `Omega` → Ω.
- `nabla` / `grad` → ∇.

## Встроенные автозамены MathLive

Ниже перечислен полный активный текстовый набор MathLive в mathNote. Метка «сразу» означает ввод без `Enter`; остальные группы требуют `Enter`.

### Функции — сразу

- `tg` → tg, `sh` → sh, `ch` → ch, `th` → th.
- `ln` → ln, `lg` → lg, `lb` → lb.
- `Re` → Re, `Im` → Im.
- `sin`, `cos`, `tan`, `cot`.
- `log` → `\log_{□}`, `exp`.
- `arcsin`, `arccos`, `arctan`.

### Функции — Enter

- `sec`.
- `ctg` / `cotg`; `csc` / `cosec`.
- `sinh`, `cosh`, `tanh`; `coth` / `cth`; `sech`.
- `arctg`, `arcsec`, `arccsc`.
- `arsinh`, `arccosh` / `arcosh`, `artanh` / `arctanh`, `arcsech`, `arccsch`.
- `arg`.
- `liminf`, `limsup`.
- `argmin`, `argmax`.
- `max`, `min`, `det`, `mod`, `(mod`.
- `gcd`, `lcm`, `erf`, `erfc`, `fft`.
- `mean`, `median`, `bessel`.
- `randomReal`, `randomInteger`.
- `sint` → `\sin t`.

### Другие конструкции — Enter

- `prod` → `\prod_{□}^{□}`.
- `cbrt` → `\sqrt[3]{□}`.
- `ceil` → `\left\lceil□\right\rceil`.
- `floor` → `\left\lfloor□\right\rfloor`.

### Логика и множества

- ∧: `&&` / `^^` — сразу; `and` — через `Enter`.
- ∨: `or` / `vv` — сразу.
- ∀: `AA` — сразу; `forall` — через `Enter`.
- ∃: `EE` — сразу; `exists` — через `Enter`.
- ∄: `Alt+Shift+E` / `!EE` — сразу; `!exists` — через `Enter`.
- ∈: `Shift+'` — сразу; `in` — через `Enter`.
- ∉: `Shift+Alt+'` / `!in` — сразу.
- ∪: `uu` — сразу; `union` — через `Enter`.
- ∩: `nn` — сразу.
- ×: `xx` — сразу; `times` — через `Enter` (умножение или декартово произведение).
- Через `Enter`: `uuu` → ⋃; `nnn` → ⋂; `^^^` → ⋀; `vvv` → ⋁; `setminus` → ∖.
- `ems` → ∅ через `Enter`.
- `NN` → ℕ, `ZZ` → ℤ, `QQ` → ℚ, `RR` → ℝ, `CC` → ℂ.
- `TT` → ⊤.
- Через `Enter`: `not` → ¬; `sub` → ⊂; `sup` → ⊃; `sube` → ⊆; `supe` → ⊇; `aleph` → ℵ; `_|_` → ⊥; `|--` → ⊢; `|==` → ⊨.

### Стрелки и сравнения

- →: `->` — сразу; `rarr` / `approaches` — через `Enter`.
- ⇒: `=>` — сразу; `rArr` — через `Enter`.
- ⇔: `Alt+=` / `iff` — сразу; `<=>` / `hArr` — через `Enter`.
- ↔: `<->` / `harr` — через `Enter`.
- ≤: `le` / `<=` — сразу; `lt=` — через `Enter`. `le` добавлен mathNote.
- ≥: `ge` / `>=` — сразу; `gt=` — через `Enter`. `ge` добавлен mathNote.
- ≺: `-<` — сразу; `-lt` — через `Enter`.
- Сразу: `!=` → ≠; `<<` → ≪; `>>` → ≫; `~~` → ≈; `?=` → ≟; `+-` → ±; `-=` → ≡; `~=` → ≅; `lt` → <; `gt` → >.
- Через `Enter`: `-->` → ⟶; `<--` → ⟵; `==>` → ⟹; `|->` → ↦; `-<=` → ≼; `>-=` → ≽.
- `uarr` → ↑, `darr` → ↓.
- `larr` / `lArr` → ← / ⇐.
- `>->` → ↣.
- `->>` → ↠.
- `>->>` → ⤖.
- `prop` → ∝.

### Операции и символы

- ∞: `oo` — сразу, когда подходит контекст; `infty` / `infinity` — через `Enter`.
- ÷: `-:` — сразу; `divide` — через `Enter`.
- Сразу: `**` → ⋆; `||` → ‖; `//` → /; `@` / `of` → ∘.
- Сразу: `&` → `\&`; `%` → `\%`; `$` → `\$`; `''` → ″; `:=` → ≔; `::` → ∷; `(:` → ⟨; `:)` → ⟩; `:.` → ∴; `|~` → ⌈; `~|` → ⌉.
- Через `Enter`: `|><` → ⋉; `><|` → ⋊; `|><|` → ⋈.
- Через `Enter`: `(.)` → ⊙; `(+)` → ⊕; `(*)` → ⊗; `(-)` → ⊖; `(/)` → ⊘.
- Через `Enter`: `diamond` → ◇; `square` → □; `'''` → ‴; `''''` → четыре штриха; `|__` → ⌊; `__|` → ⌋.
- Через `Enter`: `...` → …; `+...` / `-...` → горизонтальное многоточие после знака; `->...` / `-->...` → стрелка с многоточием.

### Константы, дифференциалы и единицы — сразу

Эти сокращения зависят от контекста слева:

- `ii` → мнимая единица \(i\).
- `jj` → мнимая единица \(j\).
- `ee` → число \(e\).
- `dx`, `dy`, `dt` → дифференциал.
- `deg` → ° после числа.
- `xin` → \(x\in\).
- `mm`, `cm`, `km`, `kg`, `ft`, `mi` → единицы измерения.

Через `Enter`:

- `inch` → единица inch.

## Освобождённые сочетания

mathNote их больше не перехватывает:

- `Shift+Alt+N/Z/R/Q/C`.
- `Shift+Alt+<` и `Shift+Alt+>`.
- `Alt+Shift+=`.
- `Alt+A`.
- `Alt+E`.
- `Alt+C`.
- `Alt+I`.
- `Alt+U`.
- `Alt+X`.

## Важные отличия от стандартного MathLive

- Большинство встроенных сокращений длиной от 3 символов подтверждаются через `Enter`; исключения перечислены в главном правиле выше.
- `in` подтверждается через `Enter`, а `!in`, `!EE`, `deg` и `xin` работают мгновенно.
- `*` переопределён с `\cdot` на `\ast`.
- `sqrt` создаёт и показатель, и подкоренное выражение; пустой показатель исчезает при переходе внутрь корня.
- `int` и `sum` создают оба предела.
- `pi` и другие короткие греческие сокращения подтверждаются через `Enter`.
- `del` в mathNote означает δ, а не встроенный MathLive `\partial`.
- `phi` в mathNote означает `\varphi`.
